import React from 'react';
import { Box, Typography } from '@mui/material';

const Gallery: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Face Gallery
      </Typography>
      <Typography>Gallery page - Coming soon!</Typography>
    </Box>
  );
};

export default Gallery;
