import React from 'react';
import { Box, Typography } from '@mui/material';

const Capture: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Capture Faces
      </Typography>
      <Typography>Capture page - Coming soon!</Typography>
    </Box>
  );
};

export default Capture;
